To run the project follow the below steps.

1. Install JDK and Maven
2. Add JDK & Maven path system variable.
3. Download the project from git.
4. Open command prompt and switch to the project location
5. Hit the command "mvn clean install"